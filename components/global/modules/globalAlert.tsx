import { GlobalAlertType } from 'types/sanity'
import { clsx } from 'clsx'

const GlobalAlert = ({ data }: { data: GlobalAlertType }) => {
  console.log(data)
  return <section></section>
}

export default GlobalAlert
